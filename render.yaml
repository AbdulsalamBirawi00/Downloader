# Render.com configuration file for easy deployment
services:
  - type: web
    name: telegram-video-bot
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
    plan: free
